# Brand assets

- **`icon.svg`** — edit in design tools, then regenerate PNGs via `pnpm --filter a11y-studio run icon:render` in the monorepo (reads this file when `media/` submodule is present).
- **`icon-128.png`** — VS Code `package.json` `"icon"` and website header.
- **`icon-512.png`** — marketing only (not bundled in VSIX).
